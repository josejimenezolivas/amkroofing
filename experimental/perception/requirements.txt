# Core stack (acquisition, georeferencing, polygonization, viz, export)
numpy>=1.24
opencv-python-headless>=4.8
shapely>=2.0
pillow>=10.0
requests>=2.31

# Production segmentation backend (SAMSegmenter). Heavy; needs a GPU for speed.
# Install separately when you want real detection:
#   pip install segment-geospatial torch torchvision
# segment-geospatial
